# easy-nvim

一个基于 `lazy.nvim` 的轻量 Neovim 配置，按模块拆分插件文件，方便维护和扩展。

## 目录结构

- `init.lua`：入口文件，加载基础配置、快捷键和插件模块。
- `lua/basic.lua`：基础编辑行为（行号、缩进、剪贴板、分屏等）。
- `lua/keymaps.lua`：常用快捷键映射。
- `lua/plugins/init.lua`：统一注册插件模块。
- `lua/plugins/*.lua`：按功能拆分的插件配置文件。
- `lazy-lock.json`：插件版本锁定文件。

## 已配置插件（按模块）

- 主题：`tokyonight`
- 文件树：`neo-tree`
- 状态栏：`lualine`
- 标签栏：`bufferline`
- 搜索：`telescope`
- 语法高亮：`treesitter`
- LSP 管理：`mason`、`mason-lspconfig`、`lspconfig`
- 补全：`nvim-cmp`
- 其他增强：`dashboard`、`indent-blankline`、`autopairs`

## 使用方法

1. 安装 Neovim（建议较新版本）。
2. 将仓库放到 `~/.config/nvim`：
   - `git clone git@github.com:yangwan-cw/easy-nvim.git ~/.config/nvim`
3. 启动 Neovim：`nvim`
4. 首次启动会自动安装 `lazy.nvim` 和插件。

## 常用快捷键

- `<leader>w`：保存
- `<leader>q`：退出当前窗口
- `<leader>Q`：强制退出全部
- `<C-h/j/k/l>`：窗口切换
- `<A-Left/Right/Up/Down>`：调整窗口大小
- `<leader>h`：清除搜索高亮

## 说明

- 当前分支已改为按 `lua/plugins/` 目录进行插件拆分管理。
- 若新增插件，建议在 `lua/plugins/` 中单独建文件，并在 `lua/plugins/init.lua` 中注册。
